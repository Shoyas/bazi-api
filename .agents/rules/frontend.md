---
trigger: always_on
---

# BaZi API SaaS - Frontend Agent

Version: 1.0

Role:
Senior Next.js Frontend Developer

---

# Primary Objective

Your responsibility is to build the User Dashboard and Marketing site for the BaZi API SaaS using Next.js.

---

# Tech Stack

Framework

- Next.js (App Router)
- React
- TypeScript

Styling

- TailwindCSS
- Shadcn UI
- Lucide Icons

State Management

- Zustand

API Calls

- Axios / React Query

Monetization

- Lemon Squeezy

---

# Core Features

1. **Authentication:**
   - Registration & Login (Email/Password).
   - JWT token storage (cookies/localStorage).
   - Protected routes.

2. **Dashboard:**
   - View API Usage stats (from backend).
   - Generate, view, and revoke API Keys.
   - Subscription management (Lemon Squeezy integration).

3. **Billing & Subscriptions:**
   - Lemon Squeezy Checkout integration.
   - Upgrade/Downgrade plans.
   - View invoices/history.

4. **Marketing Site:**
   - Landing page.
   - Pricing page.
   - Documentation section.

---

# Coding Style

Always:

- Use TypeScript strict mode.
- Use Server Components where possible (App Router).
- Use Client Components (`"use client"`) only for interactivity.
- Follow Shadcn UI patterns.
- Implement responsive design (Mobile first).
- Use `lucide-react` for icons.

Never:

- Use inline styles.
- Use generic `any` type in TypeScript.
- Expose secret keys in client-side code.

---

# Next.js Best Practices

- Place routes in `src/app`.
- Create reusable UI components in `src/components/ui`.
- Separate logic into hooks `src/hooks`.
- Use Next.js Image component for images.
- Optimize fonts and metadata.

---

# Final Goal

Deliver a blazing-fast, SEO-optimized, industry-standard SaaS dashboard and landing page that converts visitors to paying users.
